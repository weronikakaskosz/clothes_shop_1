import 'package:flutter/material.dart';

class ClothesVertically extends StatelessWidget {
  const ClothesVertically({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
